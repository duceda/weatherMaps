import { IMapCoordinates } from '../interfaces/map.interfaces';
export class MapConstants {
  static readonly DEFAULT_CENTER_COORDINATES: IMapCoordinates = {
    lat: -37.87,
    lang: 175.475
  };

  constructor() {}
}
